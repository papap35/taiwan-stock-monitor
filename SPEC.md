# TaiFin 台股資訊站 — 功能規格書 SPEC.md

> 以專業波段交易員視角撰寫。  
> 優先順序依「對實際交易決策的影響程度」排列。

---

## 現有功能盤點

| 模組 | 已完成 |
|---|---|
| 大盤總覽 | TAIEX 分時/走勢、市場廣度、漲停/跌停家數、類股表現、重點個股、國際市場 |
| 熱門股票 | 成交量/漲幅/跌幅/漲停排行 |
| 自選股 | 多筆買入記錄、加權均成、損益計算（整張+零股）、AI 每日簡報 |
| K 線圖 | 日K + KD / RSI / MACD，法人籌碼、融資融券、本益比 |
| 籌碼分析 | 三大法人市場排行、個股查詢 |
| 警報系統 | 停損/突破/跌破/買賣訊號，LocalStorage 持久化 |
| AI 分析 | Claude Sonnet 個股分析 + 每日持倉簡報 + 大盤解讀 |
| 資料管理 | JSON 匯出/匯入、CSV 匯出、快照版本管理 |
| 首頁儀表板 | 9 個可開關的客製化面板 |
| 基礎設施 | WebSocket 即時推播、REST 保底輪詢、Vite 分包、87 個單元測試 |

---

## 待開發功能規格

---

### P0 — 交易安全（必做，直接影響實際獲損）

#### 1. 動態停損追蹤（Trailing Stop）`[x]`

**背景**：固定停損只能保護下跌，無法鎖住獲利。波段交易員最需要的是「賺了錢之後，回檔超過 X% 才賣」。

**功能規格**：
- 每筆 Lot 可設定「移動停損比例」（例如：從高點回檔 8% 出場）
- 後端持續追蹤每支持股的盤中高點（`peakPrice`），超過歷史高點時更新
- 計算動態停損價 = `peakPrice × (1 - trailingPct / 100)`
- 警報觸發：現價跌破動態停損價時推播
- UI：自選股行顯示「動態停損價」欄位，用進度條表示距停損距離

**資料欄位**（新增於 Lot）：
```js
trailingStopPct:   number | null  // 移動停損百分比，null = 不啟用
peakPrice:         number         // 追蹤的歷史高點（後端記錄）
trailingStopPrice: number         // 即時計算的停損觸發價
```

---

#### 2. 風險/報酬比計算器（R/R Ratio）`[x]`

**背景**：專業交易員每筆交易前必做：`期望報酬 / 最大風險 >= 2`，低於此比例不應進場。

**功能規格**：
- 在「新增買入記錄」Modal 新增「規劃出場」區塊
  - 目標價（已有）
  - 停損價（已有）
  - 自動計算：**風險報酬比**、最大虧損金額、目標獲利金額
- 公式：`RR = (目標價 - 成本) / (成本 - 停損價)`
- 建議：RR < 1.5 → 橘色警示 `「風險偏高，建議調整停損或目標」`；RR >= 2 → 綠色 `「風險報酬合理」`

---

#### 3. 部位規模計算器（Position Sizing）`[x]`

**背景**：Kelly Criterion 或固定風險比例，避免單筆交易賠掉過多本金。「活著才能繼續交易」。

**功能規格**：
- 設定頁新增：「總資金」與「單筆最大風險比例」（預設 2%）
- 輸入股價 + 停損價 → 自動算出「建議買入張數」
- 公式：`建議股數 = (總資金 × 風險比例) / (股價 - 停損價)`
- 顯示：建議張數、實際最大虧損金額、占總資金比例
- 位置：新增買入 Modal 下方（折疊式展開）

**實作備註**：「設定」頁「顯示設定」卡片新增「總資金」「單筆最大風險比例」輸入欄位（[Settings.jsx](frontend/src/components/Settings.jsx)），新增買入 Modal 已串接 `calcPositionSize` 顯示建議張數與最大虧損（[Watchlist.jsx](frontend/src/components/Watchlist.jsx)）。

---

### P1 — 技術分析強化（影響進出場判斷品質）

#### 4. 布林通道（Bollinger Bands）`[x]`

**背景**：最常與 KD、RSI 搭配使用的趨勢/震盪判斷工具，判斷是否超漲超跌。

**功能規格**：
- 在 K 線圖主圖疊加布林通道（中軌 MA20、上軌 +2σ、下軌 -2σ）
- 計算邏輯（純前端，無需後端）：
  ```
  中軌 = SMA(20)
  標準差 σ = sqrt( Σ(close - SMA)² / 20 )
  上軌 = 中軌 + 2σ
  下軌 = 中軌 - 2σ
  帶寬 = (上軌 - 下軌) / 中軌   // 越窄越容易噴出
  ```
- 主圖顯示三條線（半透明填色區間）
- K 線圖工具列新增「BB」切換按鈕，預設關閉

---

#### 5. 成交量均線（Volume MA）與量比 `[x]`

**背景**：「量是價的先行指標」——放量突破才是真突破，縮量反彈要小心。

**功能規格**：
- K 線子圖（Volume Bar）疊加 MA5 / MA20 均量線
- 每根量柱顯示顏色：放量（> MA5 × 1.5）→ 鮮紅/鮮綠；縮量 → 半透明
- 圖表右上角顯示「量比」= 今日量 / MA5，數值 > 1.5 標紅

---

#### 6. 多週期 K 線（週K / 月K）`[x]`

**背景**：波段交易必看週線確認趨勢方向，再看日線找進場點，「順大趨勢做小波段」。

**功能規格**：
- K 線圖工具列新增週期切換：日K / 週K / 月K
- 週K：每週最後交易日收盤為 close，週內最高/最低為 high/low，週一開盤為 open
- 月K：同上邏輯，以月為單位
- 資料來源：從現有 TWSE STOCK_DAY 歷史資料在**前端**聚合計算，不增加後端 API

---

#### 7. 均線系統完整化 `[x]`

**背景**：台灣股市最常用 5/10/20/60/120/240 日均線，月線/季線/半年線/年線各有意義。

**功能規格**：
- [x] 主圖補齊 MA10 / MA120 / MA240
- [x] 各均線獨立開關（工具列按鈕）
- [x] 顏色系統標準化：MA5=白、MA10=黃、MA20=橘、MA60=紫、MA120=藍、MA240=紅
- [x] 顯示各均線當前值（統計列顯示，含站上/跌破標示）
- [-] hover tooltip（lightweight-charts 限制，改以統計列常駐顯示）

---

#### 8. AI K 線型態辨識 `[x]`

**背景**：型態分析需要大量經驗，AI 可以輔助初學者和繁忙的交易員。

**功能規格**（整合 Claude API）：
- K 線圖新增「AI 型態分析」按鈕
- 將最近 60 根 K 棒的 OHLCV + 目前技術指標值送給 Claude
- Claude 回傳：
  - 識別到的圖形型態（頭肩頂/底、W底/M頭、旗形、三角收斂、杯柄型態...）
  - 型態完成度（%）與可信度
  - 預期突破方向與目標價位
  - 失效條件（型態破壞的停損位）
- 結果以 K 線圖右側面板顯示

---

### P2 — 籌碼深度分析（判斷主力意圖）

#### 9. 外資期貨淨部位 `[x]`

**背景**：外資在期貨市場的多空部位是大盤方向的**領先指標**，比現貨更早反映機構看法。

**功能規格**：
- 資料來源：台灣期交所 `taifex.com.tw`（公開，免費）
- 顯示：外資台指期淨多空口數、未平倉口數、當日變化量
- 位置：大盤總覽新增「期貨籌碼」卡片
- 警示：外資期貨單日轉空超過 5000 口 → 紅色警告

---

#### 10. 個股籌碼評分系統 `[x]`

**背景**：把多個籌碼指標量化成一個分數，快速篩選「籌碼乾淨」的股票。

**功能規格**（純前端計算，基於現有 API 資料）：
```
籌碼分數（0～100 分）：
  外資連續買超天數   × 5  （最高 25 分）
  投信買超           × 3  （最高 15 分）
  自營商買超         × 2  （最高 10 分）
  融資餘額減少       × 3  （籌碼乾淨，最高 15 分）
  融券增加（軋空）   × 2  （最高 10 分）
  法人合計買超佔比   × 5  （最高 25 分）
```
- 自選股行新增「籌碼分」欄位（0-100 + 顏色深淺）
- 熱門股列表新增「籌碼分」排序選項
- 點擊分數展開各項明細

---

#### 11. 大盤融資融券趨勢 `[x]`

**背景**：整體市場融資水位高 → 散戶過度槓桿 → 容易崩跌。

**功能規格**：
- 大盤總覽新增「信用交易」面板
  - 全市場融資餘額（億元）趨勢圖（近 20 日）
  - 全市場融券餘額趨勢圖
  - 券資比 = 融券/融資（軋空力道指標）
- 資料來源：TWSE `MARGIN_PURCHASE_SHORT_SALE` 聚合

---

### P3 — 交易紀錄與績效（長期進步的關鍵）

#### 12. 交易日誌（Trade Journal）`[x]`

**背景**：沒有記錄就沒有進步。專業交易員每筆交易都有記錄，用於覆盤。

**功能規格**：
- Lot 新增出場欄位（選填）：
  ```js
  exitPrice:  number   // 出場價格
  exitDate:   string   // 出場日期
  exitReason: string   // 出場理由（停損/目標到/技術破壞/其他）
  entryNote:  string   // 進場理由（原有 note 重命名）
  lesson:     string   // 這筆學到什麼
  ```
- 填入 `exitPrice` 後該筆 Lot 自動進入「已出場」狀態
- 已出場顯示：實現損益、持有天數、年化報酬率

---

#### 13. 績效統計儀表板 `[x]`

**背景**：量化過去操作，客觀評估策略是否有效。

**功能規格**（計算已出場 Lot）：
```
核心指標：
  總交易筆數  獲利筆數  勝率（%）
  平均獲利    平均虧損  獲利因子（= 平均獲利/平均虧損，目標 > 2）
  期望值      最大連勝  最大連敗
  最大單筆獲利 最大單筆虧損
```
- 月報酬熱力圖（仿 GitHub contribution 圖）
- 個股勝率排行（找出你最熟悉的標的）
- 策略標籤勝率（長期/波段/短線 各自績效）
- 資金曲線（每筆出場後的累積損益折線圖）

---

#### 14. AI 覆盤助手 `[x]`

**背景**：出場後讓 AI 幫分析這筆交易，找出系統性錯誤。

**功能規格**：
- 已出場 Lot 顯示「AI 覆盤」按鈕
- 送入：進出場價格/日期、理由、K 線資料、技術指標
- Claude 回傳：
  - 進場時機評估（位置好不好、依據是否充分）
  - 出場時機評估（過早/過晚/合理）
  - 如果重來該怎麼操作
  - 這類錯誤的系統性改進方向

---

### P4 — 選股工具（主動找標的）

#### 15. 條件選股掃描器（Stock Scanner）`[x]`

**背景**：主動找符合條件的股票，而非被動等消息或靠運氣。

**功能規格**：
- 篩選條件（可組合，AND 邏輯）：
  - **技術面**：KD 黃金交叉、RSI < 30、突破 60 日高點、站上季線、縮量盤整後放量
  - **籌碼面**：外資連 3 日買超、投信連續買超、融資減少
  - **基本面**：PE < 15、殖利率 > 5%、PB < 1.5
  - **量能**：量比 > 1.5、日成交量 > N 億
- 掃描範圍：輸入股票代號清單（可貼上多個）
- 掃描結果：表格顯示 + 各指標數值 + 一鍵加入自選股

---

#### 16. 自選股分群管理 `[x]`

**背景**：持股、觀察池、候選清單混在一起，難以區分狀態和優先順序。

**功能規格**：
- 自選股新增「分群」Tab：
  - 我的持股（預設）
  - 觀察中
  - 候選清單
  - 空頭觀察
  - 自訂群組（可新增/重命名/刪除）
- 股票可跨群組移動（右鍵選單或拖曳）
- 各群組顯示獨立統計（如持股群組顯示總損益）

---

### P5 — 基礎設施（長期使用必備）

#### 17. 雲端同步（Supabase）`[x]`

**背景**：目前資料存在 LocalStorage，換裝置或清瀏覽器資料消失。

**已實作**：
- Supabase PostgreSQL（4 張表：watchlist / groups / alerts / settings）
- 同步資料：watchlist、lots、alerts、settings、groups
- 策略：**離線優先**（LocalStorage 為主，手動 push/pull 到 Supabase）
- 後端作為中介（`/api/sync/*`），key 不暴露前端
- 設定頁提供「⬆ 推送到雲端」/ 「⬇ 從雲端還原」按鈕
- 顯示上次同步時間
- migration SQL：`supabase_migration.sql`

---

#### 18. LINE Notify 警報推播 `[x]`

**背景**：瀏覽器通知容易錯過；LINE 是台灣最普及的即時通訊，開盤時最常用手機。

**功能規格**：
- 設定頁新增「LINE Notify 連結」
  - 引導使用者到 `notify-bot.line.me` 取得個人 token
  - 貼上 token 後儲存（加密存於後端或 LocalStorage）
- 後端警報引擎觸發時，呼叫 LINE Notify API
- 訊息格式：
  ```
  [台股警報] ⚠️ 台積電 2330
  現價：850 (-5.56%)
  觸發停損（成本 900，虧損 -50,000 元）
  時間：13:21:05
  ```
- 每日簡報也可透過 LINE 發送（08:45 開盤前 / 13:35 收盤後）

---

#### 19. 重要事件行事曆（除權息/財報）`[x]`

**背景**：持有股票必須提前知道除息日（填息策略）和財報公布日（避開地雷）。

**功能規格**：
- 資料來源：TWSE 公開資訊觀測站（免費，HTML 解析）
- 自選股有相關事件時，股票行顯示提醒標籤：
  - `📅 3天後除息 $3.2`
  - `📋 5天後法說會`
- 首頁儀表板新增「近期事件」面板（未來 30 天）
- 除息前 3 天 LINE 推播提醒（需開啟 LINE Notify）

---

#### 20. 盤前/盤後自動 AI 簡報排程 `[x]`

**背景**：目前 AI 簡報需手動觸發，真正有用的是**主動推送到 LINE**。

**功能規格**：
- 後端 cron 排程（結合 LINE Notify）：
  - `08:45` → 開盤前分析（結合前日法人籌碼 + 國際市場）
  - `13:35` → 收盤後檢討（結合當日漲跌 + 持股損益）
- 同現有 AI 簡報格式，加入當日市場背景
- 使用者可在設定頁開啟/關閉每日自動推送

---

### P6 — 帳戶與進階分析

#### 21. Google 登入（Supabase Auth）`[x]`

**背景**：目前雲端同步寫死 `user_id = 'default'`，所有使用者共用同一筆雲端資料，無法真正做到多裝置／多使用者隔離，且 RLS 形同虛設。

**已實作**：
- 前端 `services/supabaseClient.js`：僅供 Auth 使用（登入狀態、access token），資料讀寫仍一律透過 `/api/sync/*`
- `hooks/useAuth.js`：`signInWithGoogle` / `signOut` / `getAccessToken`，依 `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` 是否設定決定 `enabled`
- `services/api.js`：新增 `setAuthTokenGetter`，所有 API 請求自動帶上 `Authorization: Bearer <access token>`（未登入則不帶）
- `hooks/useCloudSync.js`：登入狀態變化時自動 `pull()` 該使用者的雲端資料
- 設定頁雲端同步區塊新增登入狀態顯示（email/頭像）與「使用 Google 登入」/「登出」按鈕；未設定 Supabase Auth 時不顯示
- 後端 `middleware/auth.js`（`resolveUserId`）：解析 `Authorization` header 中的 Supabase JWT → `req.userId`；無/無效 token 時 fallback 為 `DEFAULT_USER_ID = 'default'`（向下相容單用戶模式）
- `services/supabase.js`：所有 pull/push 函式改為以 `userId` 參數隔離資料
- `supabase_migration.sql`：4 張表 PK 改為 `(id, user_id)`（同代號可分屬不同使用者）、新增 RLS policy（`auth.uid()::text = user_id`）與既有環境升級 SQL
- 測試：後端 `auth.test.js`（5 tests）、前端 `useAuth.test.js`（3 tests）

**手動設定（需使用者自行完成）**：
- Supabase Dashboard → Authentication → Providers 啟用 Google，並填入 Google Cloud OAuth Client ID/Secret
- 前端 `.env` 設定 `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`
- 既有環境執行 `supabase_migration.sql` 中的升級 SQL（調整 PK 為 `(id, user_id)` 並啟用 RLS）

---

#### 22. 庫存/損益總覽（Portfolio）`[x]`

**背景**：自選股目前偏向「關注清單」，沒有持有成本與損益總覽，無法一眼看出資產配置與整體績效。

**已實作**：
- 「庫存總覽」分頁：
  - 總市值、總成本、未實現損益（金額 / %）
  - 各持股市值佔比圓餅圖（資產配置）
  - 各持股未實現損益排行
- 已出場（含 exitPrice）的 lot 併入已實現損益計算
- 使用手冊：[USER_MANUAL.md 第十六章](USER_MANUAL.md#十六庫存總覽portfolio)

---

#### 23. 選股條件回測 `[x]`

**背景**：條件選股掃描器（P4-15）僅能即時掃描，無法驗證條件本身是否有效。

**已實作**：
- 掃描結果新增「回測（近 1 個月）」欄位，可選擇持有天數（1/5/10 個交易日）
- 僅支援只需日K資料的技術面/量能條件（RSI、突破 60 日高點、站上季線、量比、KD 黃金交叉），逐日套用條件並計算其後續 N 個交易日報酬（[scanner.js](frontend/src/utils/scanner.js) `runBacktest`）
- 結果顯示：選中次數、勝率、平均報酬、最大回撤（依序疊加交易報酬模擬權益曲線計算）
- 籌碼面/基本面條件因無歷史每日資料無法回測，掃描器會自動排除並提示
- 點擊「執行回測」即時抓取近 6 個月日K計算（前端純函式運算，無需背景工作）

---

#### 24. AI 週報摘要 `[x]`

**背景**：已有盤前/盤後 AI 簡報（P5-20），但缺少週期性整理，難以掌握中期趨勢。

**已實作**：
- 每週五 14:30（台灣時間，收盤後）自動產生「本週自選股總結」，「設定」頁「自動 AI 簡報排程」卡片新增「AI 週報摘要」開關與立即觸發按鈕（[Settings.jsx](frontend/src/components/Settings.jsx)）
- 內容：本週大盤走勢、自選股本週漲跌幅排行（取自 Supabase 自選股清單，逐檔以 `fetchHistory` 計算近 5 個交易日漲跌幅）、三大法人籌碼變化、下週 7 日內關注事件（連結行事曆 P5-19）
- 沿用現有 AI 分析架構（`claude-haiku-4-5`），格式比照每日簡報（[scheduler.js](backend/src/services/scheduler.js)、[reportHelpers.js](backend/src/utils/reportHelpers.js)）
- 透過 LINE Notify 推播（`buildReportMessage('weekly', ...)`）
- 若 Supabase 未設定或自選股為空，自選股漲跌排行與相關事件區塊會省略，僅呈現大盤與法人資訊

---

### P7 — 進階看盤功能（對標主流看盤系統的差異化補強）

> 依「相對好做 → 需多資料源整合 → 進階差異化」排序，建議依序開發：25 → 26 → 28 → 31 → 27 → 35 → 33 → 36 → 34 → 30 → 29 → 32

#### 25. K 線繪圖工具（趨勢線 / 水平線標註） `[x]`

**背景**：目前 K 線圖僅顯示均線 / KD / MACD 等技術指標，無法手動標註支撐壓力位或趨勢線。XQ、CMoney 等主流看盤系統皆支援手繪標註並保存，是判斷進出場時機的常用工具。

**功能規格**：
- K 線圖右上新增「繪圖模式」開關，開啟後點擊圖表兩點繪製水平線或趨勢線
- 標註資料依股票代號存於 localStorage（key: `chart_annotations_<code>`），結構：
  ```js
  { type: 'horizontal' | 'trendline', points: [{ time, price }], color: string, id: string }
  ```
- 切換股票或 K 線週期時自動載入對應標註並重繪
- 標註線需隨圖表縮放 / 平移正確定位（沿用 recharts `ReferenceLine` / 自訂 SVG overlay）
- 可刪除單一標註（點擊標註顯示刪除按鈕）、提供「清除全部標註」按鈕
- 後續可選：登入後同步至 Supabase（沿用 P5-17 既有同步機制）

**已實作**：
- 純函式邏輯抽至 `frontend/src/utils/chartAnnotations.js`（`annotationStorageKey` / `createAnnotation` / `addAnnotation` / `removeAnnotation`），含完整測試
- 圖表底層為 lightweight-charts v5，水平線使用 `series.createPriceLine()`、趨勢線使用 `chart.addSeries(LineSeries, ...)`（非 recharts ReferenceLine）
- K 線 Tab 工具列新增「－ 水平線」「／ 趨勢線」繪圖模式按鈕，啟用後透過 `chart.subscribeClick()` 擷取點擊座標並用 `coordinateToPrice()` 換算成價格
- 圖表右上角顯示已標註清單，每筆可點 × 個別刪除；工具列提供「清除標註 (N)」一次清空全部
- 切換股票時自動從 localStorage 載入對應標註並重繪

---

#### 26. 多股同列比較（疊圖） `[x]`

**背景**：觀察自選股相對大盤或同族群表現時，需要把多檔股票的漲跌幅 % 疊在同一張圖比較相對強弱，這是判斷「強勢股」的常用手法。

**功能規格**：
- 自選股清單新增「加入比較」勾選操作，最多選 5 檔（含可選「加入大盤 TAIEX」）
- 新增「比較」頁籤，圖表以「期初基準日 = 0%」正規化顯示各標的漲跌幅曲線
- 提供區間切換：1 個月 / 3 個月 / 6 個月 / 1 年
- 沿用既有 `fetchHistory`，前端逐檔正規化後以 recharts `LineChart` 多 series 呈現
- 圖例可切換顯示 / 隱藏各條線；滑過圖表顯示當日各標的漲跌幅 tooltip

**已實作**：
- 純函式邏輯抽至 `frontend/src/utils/compareChart.js`（`normalizeSeries` / `mergeSeries`），含完整測試
- 新增獨立頁籤「📐 股票比較」(`frontend/src/components/StockCompare.jsx`)，從自選股勾選最多 5 檔
- 區間切換 1M/3M/6M/1Y，沿用 `api.getHistory(code, months)`，前端正規化為「期初 = 0%」後用 recharts `LineChart` 多 series 疊圖
- 圖例可點擊切換顯示/隱藏個別股票線；選取清單存於 localStorage（`compare_selected_codes`）
- 「加入大盤 TAIEX」延伸為新項目 #37（TWSE 無對應股票代號歷史 API，需另外串接指數歷史資料來源，資料來源待確認）

---

#### 27. 個股新聞與重大訊息公告 `[ ]`

**背景**：看盤系統通常會在個股頁顯示近期相關新聞或公司重大訊息（股利公告、減資、併購、法說會），目前本專案缺少此資訊來源，使用者需另外查證。

**功能規格**：
- 後端新增 `/api/stocks/:code/announcements`，抓取公開資訊觀測站（MOPS）重大訊息公告
- 快取策略：沿用 `cacheDaily`（5 分鐘 TTL）
- 個股 K 線頁新增「重大訊息」清單區塊，顯示日期 + 標題，點擊展開完整內容
- 若該日有公告，K 線圖對應日期標記小圖示（hover 顯示標題）
- API 來源不穩定時需 graceful fallback（沿用 twse.js 既有錯誤處理慣例），不可造成個股頁整體載入失敗

---

#### 28. 瀏覽器推播通知（Web Push） `[x]`

**背景**：警報系統（P5-18）目前僅支援 LINE Notify，使用者需切換 App 才能看到。瀏覽器原生通知可即時提醒、不需額外設定 token，適合常駐瀏覽器盤中監控的使用情境。

**功能規格**：
- 「設定」頁新增「瀏覽器推播」開關，首次開啟時請求 `Notification` permission
- 警報觸發時（價格到價、停損、突破、買賣訊號），除既有 LINE 推播外，同步呼叫 `new Notification(...)` 顯示前景通知
- 通知內容：股票名稱 + 代號、觸發條件描述、現價
- 第一階段僅支援頁面開啟時的前景通知；背景通知（需 Service Worker + PWA）標註為後續延伸項目，本項不強制實作

**已實作**：
- 純函式 `formatNotification(event)` 抽至 `frontend/src/utils/browserNotify.js`（含 `fireBrowserNotifications`），含 9 個測試
- `useWebSocket.js` 在 `alerts_triggered` 觸發時，若 `settings.browserNotifEnabled` 為 true 且 `Notification.permission === 'granted'`，呼叫 `new Notification(title, { body, icon })` 逐筆顯示
- `Settings.jsx` 新增「瀏覽器推播通知」卡片，含 permission 狀態感知：未授權 → 開關自動呼叫 `requestPermission()`；已封鎖 → 顯示引導訊息；不支援 → 置灰
- 背景通知（Service Worker / PWA）留至後續延伸

---

#### 29. 個股財報速覽 `[ ]`

**背景**：判斷個股基本面需要 EPS、營收成長率、毛利率趨勢，目前僅有本益比 / 殖利率（來自 TWSE `BWIBBU_d`），缺乏財報細項，需要的人得另外查 Goodinfo 或財報狗。

**功能規格**：
- 後端新增 `/api/stocks/:code/financials`，抓取 MOPS 月營收（每月 10 日公布）
- 個股頁新增「財報速覽」卡片：近 12 個月營收年增率（YoY）趨勢圖
- 季 EPS / 毛利率因公開免費 API 來源不穩定（多為 XBRL 或第三方付費），先標註為「資料來源待確認」，本項第一階段僅實作月營收 YoY，待確認可用來源後再擴充
- 快取：沿用 `cacheDaily`，財報資料變動慢可視情況拉長 TTL 至數小時

---

#### 30. 大盤期貨與選擇權籌碼擴充 `[ ]`

**背景**：已有外資期貨淨部位（P2-9），選擇權 Put/Call Ratio（PC Ratio）是判斷市場多空情緒的常用指標，目前缺少。

**功能規格**：
- 後端新增 `/api/market/options`，抓取台指選擇權三大法人未平倉與 PC Ratio（來源：`taifex.com.tw`，需解析 HTML，沿用現有期貨籌碼解析慣例）
- 大盤總覽頁新增「選擇權籌碼」卡片：PC Ratio 數值 + 近 5 日趨勢、三大法人選擇權多空淨額
- 快取：`cacheDaily`（盤後更新一次即可）

---

#### 31. 族群 / 概念股篩選 `[ ]`

**背景**：投資人常以「AI 概念股」「重電」「航運」等主題瀏覽，目前條件選股掃描器需手動輸入代號清單，缺少預設族群分類，每次掃描前都要自行整理清單。

**功能規格**：
- 新增前端常數檔 `frontend/src/utils/industryGroups.js`，內建 10～15 個常見概念股族群與對應代號清單（如 AI 伺服器、重電、航運、半導體設備、金融、ETF 等）
- 條件選股掃描器新增「快速代入族群」下拉選單，選擇後自動填入該族群代號至掃描代號輸入框（可與手動輸入的代號合併或取代，UI 需提示）
- 熱門股票頁可依族群篩選排行（下拉選單篩選 `industryGroups` 內的代號）
- 族群清單為靜態資料，後續可視需要改為可由使用者自訂

---

#### 32. ETF 專區 `[ ]`

**背景**：0050 / 006208 等 ETF 是許多投資人的核心持股，但目前個股查詢未特別處理 ETF 的成分股與配息資訊，使用者需另外查詢。

**功能規格**：
- 個股頁偵測代號為 ETF（0050、0056、006208 等常見市值型 ETF，先以靜態清單判斷）時，新增「ETF 資訊」卡片
- 顯示：追蹤指數名稱、近一年配息次數與金額（殖利率，沿用 `BWIBBU_d` 殖利率欄位）
- 前 10 大成分股權重因無穩定免費 API，第一階段以手動維護的靜態清單呈現（如 0050 成分股需人工每季更新），並在 UI 標註「資料更新日期」
- 快取：`cacheDaily`

---

#### 33. AI 交易日記模式辨識 `[ ]`

**背景**：已有 AI 覆盤（P3-14）逐筆分析，但缺乏「長期模式」洞察，例如：某類進場理由（`entryReason`）的勝率是否特別高或低，長期累積後才能看出個人交易習慣的優劣。

**功能規格**：
- 績效統計頁新增「進場理由分析」區塊，依既有 `entryReason` 欄位分組統計各類理由的次數、勝率、平均報酬（沿用 `calcPerformance`）
- AI 週報摘要（P6-24）新增「本週模式觀察」段落：比對本週交易與歷史同類型理由的表現，給出簡短建議文字
- 統計運算為純前端（沿用既有 `performance.js` 工具），AI 文字生成沿用既有 `claude-haiku-4-5` 架構，不新增外部依賴

---

#### 34. 籌碼異動自動標註候選清單 `[ ]`

**背景**：個股籌碼評分（P2-10）需手動查詢，若能自動篩選「連續 N 天外資買超且創新高」的股票並加入候選清單，可減少盤後人工監控的時間成本。

**功能規格**：
- 後端排程（沿用 `scheduler.js`）每日收盤後，對「觀察池」（設定頁可設定的熱門股代號清單，預設提供一份常見大型股清單）執行籌碼異動檢測
- 條件：連續 3 個交易日外資買超 且 股價創 20 日新高
- 符合條件者自動加入該使用者自選股的「候選清單」分群（沿用 P6-16 分群功能），並透過 LINE 推播提示（沿用 `buildReportMessage`）
- 「設定」頁新增「籌碼異動掃描」開關與觀察池代號清單設定欄位

---

#### 35. 多組合管理（Multiple Portfolios） `[ ]`

**背景**：目前自選股是單一清單，無法區分「長期存股」與「短線交易」等不同策略的資金與績效，混在一起會干擾庫存總覽與績效統計的判讀。

**功能規格**：
- watchlist 資料結構新增 `portfolioId` 欄位，預設值為 `'default'`
- 「自選股」頁新增「組合」切換器（與既有分群功能並存，分群用於同組合內再分類；組合切換則改變整體顯示範圍），可新增 / 重命名 / 刪除組合
- 「庫存總覽」（P6-22）與「績效統計」（P3-13）頁可依組合篩選顯示
- 資料遷移：既有資料載入時若無 `portfolioId` 欄位，自動補上 `'default'`，確保向後相容、不需手動遷移腳本

---

#### 36. 回測策略 A/B 比較 `[ ]`

**背景**：選股條件回測（P6-23）目前一次只能測試一組勾選條件，無法比較「條件組合 A」vs「條件組合 B」的優劣，難以做策略迭代。

**功能規格**：
- 掃描器新增「策略比較」模式：可建立 2～3 組獨立的條件組合（各自勾選一組 `SCAN_CONDITIONS`）
- 對同一份代號清單分別執行 `runBacktest`，結果以表格並列呈現：各策略的選中次數、勝率、平均報酬、最大回撤
- 沿用既有 `runBacktest` 純函式，僅前端 UI 調整為多組條件管理與並列顯示，不需新增後端 API

---

#### 37. 多股比較加入大盤 TAIEX `[ ]`

**背景**：P7-26 多股同列比較目前僅支援自選股之間比較，無法加入大盤 TAIEX 作為對照基準。實作時發現 TWSE `STOCK_DAY` API 僅支援個股代號，無對應的加權指數歷史資料端點，需另外確認資料來源。

**功能規格**：
- 後端新增大盤指數歷史資料端點（資料來源待確認，可能為 TWSE `MI_5MINS_HIST` 或其他每日指數收盤資料 API）
- 「股票比較」頁籤新增「加入大盤 TAIEX」勾選項，沿用既有 `normalizeSeries`/`mergeSeries` 正規化邏輯疊加顯示

---

## 技術債與基礎強化

#### T1. 測試覆蓋率提升 ✅

- **完成**：113 後端 / 182 前端，共 295 個單元測試
  - 新增 `twse.test.js`（17 tests）：mock node-fetch 測試 API 資料解析
  - 新增 `websocket.test.js`（10 tests）：collectSubscribedCodes / broadcast 邏輯
- **完成**：新增 `e2e/`（Playwright），涵蓋首頁儀表板、自選股新增/刪除、條件選股掃描、庫存總覽共 5 個測試，並整合至 CI（`e2e` job，依賴 backend/frontend job）

#### T2. 後端快取策略清晰化 ✅

- **完成**：`twse.js` 拆為三層 NodeCache
  - `cacheRealtime`：盤中報價，15 秒 TTL
  - `cacheDaily`：法人/期貨/融資券，5 分鐘 TTL
  - `cacheHistory`：歷史K線/本益比，30 分鐘 TTL

#### T3. 離線體驗與錯誤邊界 ✅

- **完成**：
  - `ErrorBoundary.jsx`：捕捉 render 錯誤，顯示友善訊息 + 重試按鈕
  - `useOnlineStatus.js`：監聽 window online/offline 事件
  - `OfflineBanner.jsx`：斷線時頂部紅色 banner，恢復後綠色提示 3 秒

#### T4. Bundle 大小優化 ✅

- **完成**：Market / Watchlist / Chips 改為 React.lazy 懶載入
- **結果**：主包 gzip 39.97KB（目標 < 100KB ✅），recharts chunk 獨立 152KB

---

## 優先開發路徑建議

```
第一階段（交易安全，立即影響）          預估工時
  P0-1  動態停損追蹤                    2天
  P0-2  風險/報酬比計算器               0.5天
  P0-3  部位規模計算器                  0.5天

第二階段（技術分析提升）
  P1-4  布林通道                        1天
  P1-5  成交量均線 + 量比               1天
  P1-6  多週期 K 線（週K/月K）          1天

第三階段（交易紀錄，長期進步）
  P3-12 交易日誌（出場記錄）            2天
  P3-13 績效統計儀表板                  2天

第四階段（基礎設施）
  P5-17 Supabase 雲端同步               3天
  P5-18 LINE Notify 推播                1天
```

---

## 附錄：免費資料來源清單

| 資料類型 | API / 來源 | 更新頻率 | 備註 |
|---|---|---|---|
| 盤中即時報價 | `mis.twse.com.tw` | 15 秒 | 盤中限定 |
| 日K歷史 | `twse.com.tw/exchangeReport/STOCK_DAY` | 每月一份 | 需逐月抓取 |
| 三大法人 | `twse.com.tw/fund/T86` | 盤後更新 | 當日彙總 |
| 融資融券 | `twse.com.tw/.../MARGIN_PURCHASE_SHORT_SALE` | 盤後更新 | 逐月 |
| 本益比/殖利率 | `twse.com.tw/exchangeReport/BWIBBU_d` | 盤後 | 全市場 |
| 大盤分時 | `twse.com.tw/exchangeReport/MI_5MINS_INDEX` | 5 秒 | 含類股指數 |
| 期貨籌碼 | `taifex.com.tw/cht/3/futContractsDate` | 盤後 | 需解析 HTML |
| 國際指數 | `query1.finance.yahoo.com/v8/finance/chart` | 即時 | 無需認證 |
| LINE Notify | `notify-api.line.me/api/notify` | — | 個人 token 免費 |
| Supabase DB | PostgreSQL | — | 免費 500MB |
